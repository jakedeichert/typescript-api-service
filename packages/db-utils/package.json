{
  "name": "db-utils",
  "version": "0.1.0",
  "author": "Jake Deichert",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "../../node_modules/.bin/tsc --watch --preserveWatchOutput",
    "build": "npm run clean && ../../node_modules/.bin/tsc",
    "lint": "../../node_modules/.bin/eslint . --ext ts --ignore-pattern dist",
    "clean": "rm -rf dist/*"
  },
  "dependencies": {
    "knex": "^0.21.15",
    "pg": "^8.5.1"
  },
  "devDependencies": {
    "@types/pg": "^7.14.7"
  }
}
